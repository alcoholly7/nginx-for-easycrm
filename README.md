This is a docker repository of using nginx as reverse proxy to direct traffic for EasyCRM.

## build docker image

`docker build -t [your_docker_username]/nginx-for-easycrm .`

## run the container

`docker-compose up`
